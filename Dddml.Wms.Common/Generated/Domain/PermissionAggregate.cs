// <autogenerated>
//   This file was generated by T4 code generator GenerateBoundedContextDomainAggregates.tt.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

using System;
using System.Collections.Generic;
using Dddml.Wms.Specialization;
using Dddml.Wms.Domain;

namespace Dddml.Wms.Domain
{
    public partial class PermissionAggregate : AggregateBase, IPermissionAggregate
    {

        readonly IPermissionState _state;

        readonly IList<IEvent> _changes = new List<IEvent>();

        public IPermissionState State
        {
            get
            {
                return _state;
            }
        }

        public IList<IEvent> Changes
        {
            get
            {
                return _changes;
            }
        }

        #region IIdentity implementation

        string IGlobalIdentity<string>.GlobalId
        {
            get
            {
                return this._state.GlobalId;
            }
        }

        #endregion


        public PermissionAggregate(IPermissionState state)
        {
            _state = state;
        }


        public virtual void ThrowOnInvalidStateTransition(ICommand c)
        {
            if (((IPermissionStateProperties)_state).Version == PermissionState.VersionZero)
            {
                if (IsCommandCreate((IPermissionCommand)c))
                {
                    return;
                }
                throw DomainError.Named("premature", "Can't do anything to unexistent aggregate");
            }
            if (_state.Deleted)
            {
                throw DomainError.Named("zombie", "Can't do anything to deleted aggregate.");
            }
            if (IsCommandCreate((IPermissionCommand)c))
                throw DomainError.Named("rebirth", "Can't create aggregate that already exists");
        }

        private static bool IsCommandCreate(IPermissionCommand c)
        {
            return c.Version == PermissionState.VersionZero;
        }

        protected virtual void Apply(IEvent e)
        {
            OnApplying(e);
            _state.Mutate(e);
            _changes.Add(e);
        }

        public virtual void Create(ICreatePermission c)
        {
            IPermissionStateCreated e = Map(c);
            Apply(e);
        }

        public virtual void MergePatch(IMergePatchPermission c)
        {
            IPermissionStateMergePatched e = Map(c);
            Apply(e);
        }

        public virtual void Delete(IDeletePermission c)
        {
            IPermissionStateDeleted e = Map(c);
            Apply(e);
        }


        protected virtual IPermissionStateCreated Map(ICreatePermission c)
        {
			var stateEventId = new PermissionStateEventId(c.PermissionId, c.Version);
            IPermissionStateCreated e = NewPermissionStateCreated(stateEventId);
		
            e.Name = c.Name;
            e.ParentPermissionId = c.ParentPermissionId;
            e.Description = c.Description;
            e.Active = c.Active;
            ReflectUtils.CopyPropertyValue("CommandId", c, e);


            e.CreatedBy = (string)c.RequesterId;
            e.CreatedAt = DateTime.Now;
			var version = c.Version;


            return e;
        }

        protected virtual IPermissionStateMergePatched Map(IMergePatchPermission c)
        {
			var stateEventId = new PermissionStateEventId(c.PermissionId, c.Version);
            IPermissionStateMergePatched e = NewPermissionStateMergePatched(stateEventId);

            e.Name = c.Name;
            e.ParentPermissionId = c.ParentPermissionId;
            e.Description = c.Description;
            e.Active = c.Active;
            e.IsPropertyNameRemoved = c.IsPropertyNameRemoved;
            e.IsPropertyParentPermissionIdRemoved = c.IsPropertyParentPermissionIdRemoved;
            e.IsPropertyDescriptionRemoved = c.IsPropertyDescriptionRemoved;
            e.IsPropertyActiveRemoved = c.IsPropertyActiveRemoved;

            ReflectUtils.CopyPropertyValue("CommandId", c, e);


            e.CreatedBy = (string)c.RequesterId;
            e.CreatedAt = DateTime.Now;

			var version = c.Version;


            return e;
        }

        protected virtual IPermissionStateDeleted Map(IDeletePermission c)
        {
			var stateEventId = new PermissionStateEventId(c.PermissionId, c.Version);
            IPermissionStateDeleted e = NewPermissionStateDeleted(stateEventId);
			
            ReflectUtils.CopyPropertyValue("CommandId", c, e);


            e.CreatedBy = (string)c.RequesterId;
            e.CreatedAt = DateTime.Now;


            return e;
        }

        private void SetNullInnerIdOrThrowOnInconsistentIds(object innerObject, string innerIdName, object innerIdValue, string outerIdName, object outerIdValue)
        {
            if (innerIdValue == null)
            {
                ReflectUtils.SetPropertyValue(innerIdName, innerObject, outerIdValue);
            }
            else if (!Object.Equals(innerIdValue, outerIdValue))
            {
                if (innerIdValue is string && outerIdValue is string && ((string)innerIdValue).Normalize() == ((string)outerIdValue).Normalize())
                {
                    return;
                }
                throw DomainError.Named("inconsistentId", "Outer {0} {1} NOT equals inner {2} {3}", outerIdName, outerIdValue, innerIdName, innerIdValue);
            }
        }

////////////////////////

        protected PermissionStateCreated NewPermissionStateCreated(string commandId, string requesterId)
        {
            var stateEventId = new PermissionStateEventId(_state.PermissionId, ((IPermissionStateProperties)_state).Version);
            var e = NewPermissionStateCreated(stateEventId);

            e.CommandId = commandId;

            e.CreatedBy = (string)requesterId;
            e.CreatedAt = DateTime.Now;

            return e;
        }

        protected PermissionStateMergePatched NewPermissionStateMergePatched(string commandId, string requesterId)
        {
            var stateEventId = new PermissionStateEventId(_state.PermissionId, ((IPermissionStateProperties)_state).Version);
            var e = NewPermissionStateMergePatched(stateEventId);

            e.CommandId = commandId;

            e.CreatedBy = (string)requesterId;
            e.CreatedAt = DateTime.Now;

            return e;
        }


        protected PermissionStateDeleted NewPermissionStateDeleted(string commandId, string requesterId)
        {
            var stateEventId = new PermissionStateEventId(_state.PermissionId, ((IPermissionStateProperties)_state).Version);
            var e = NewPermissionStateDeleted(stateEventId);

            e.CommandId = commandId;

            e.CreatedBy = (string)requesterId;
            e.CreatedAt = DateTime.Now;

            return e;
        }

////////////////////////

		private PermissionStateCreated NewPermissionStateCreated(PermissionStateEventId stateEventId)
		{
			return new PermissionStateCreated(stateEventId);			
		}

        private PermissionStateMergePatched NewPermissionStateMergePatched(PermissionStateEventId stateEventId)
		{
			return new PermissionStateMergePatched(stateEventId);
		}

        private PermissionStateDeleted NewPermissionStateDeleted(PermissionStateEventId stateEventId)
		{
			return new PermissionStateDeleted(stateEventId);
		}


    }

}

