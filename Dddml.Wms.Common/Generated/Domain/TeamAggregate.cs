// <autogenerated>
//   This file was generated by T4 code generator GenerateBoundedContextDomainAggregates.tt.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

using System;
using System.Collections.Generic;
using Dddml.Wms.Specialization;
using Dddml.Wms.Domain;

namespace Dddml.Wms.Domain
{
    public partial class TeamAggregate : AggregateBase, ITeamAggregate
    {

        readonly ITeamState _state;

        readonly IList<IEvent> _changes = new List<IEvent>();

        public ITeamState State
        {
            get
            {
                return _state;
            }
        }

        public IList<IEvent> Changes
        {
            get
            {
                return _changes;
            }
        }

        #region IIdentity implementation

        string IGlobalIdentity<string>.GlobalId
        {
            get
            {
                return this._state.GlobalId;
            }
        }

        #endregion


        public TeamAggregate(ITeamState state)
        {
            _state = state;
        }


        public virtual void ThrowOnInvalidStateTransition(ICommand c)
        {
            if (((ITeamStateProperties)_state).Version == TeamState.VersionZero)
            {
                if (IsCommandCreate((ITeamCommand)c))
                {
                    return;
                }
                throw DomainError.Named("premature", "Can't do anything to unexistent aggregate");
            }
            if (_state.Deleted)
            {
                throw DomainError.Named("zombie", "Can't do anything to deleted aggregate.");
            }
            if (IsCommandCreate((ITeamCommand)c))
                throw DomainError.Named("rebirth", "Can't create aggregate that already exists");
        }

        private static bool IsCommandCreate(ITeamCommand c)
        {
            return c.Version == TeamState.VersionZero;
        }

        protected virtual void Apply(IEvent e)
        {
            OnApplying(e);
            _state.Mutate(e);
            _changes.Add(e);
        }

        public virtual void Create(ICreateTeam c)
        {
            ITeamStateCreated e = Map(c);
            Apply(e);
        }

        public virtual void MergePatch(IMergePatchTeam c)
        {
            ITeamStateMergePatched e = Map(c);
            Apply(e);
        }

        public virtual void Delete(IDeleteTeam c)
        {
            ITeamStateDeleted e = Map(c);
            Apply(e);
        }


        protected virtual ITeamStateCreated Map(ICreateTeam c)
        {
			var stateEventId = new TeamStateEventId(c.TeamName, c.Version);
            ITeamStateCreated e = NewTeamStateCreated(stateEventId);
		
            e.Description = c.Description;
            e.Players = c.Players;
            e.Mascots = c.Mascots;
            e.Active = c.Active;
            ReflectUtils.CopyPropertyValue("CommandId", c, e);


            e.CreatedBy = (string)c.RequesterId;
            e.CreatedAt = DateTime.Now;
			var version = c.Version;


            return e;
        }

        protected virtual ITeamStateMergePatched Map(IMergePatchTeam c)
        {
			var stateEventId = new TeamStateEventId(c.TeamName, c.Version);
            ITeamStateMergePatched e = NewTeamStateMergePatched(stateEventId);

            e.Description = c.Description;
            e.Players = c.Players;
            e.Mascots = c.Mascots;
            e.Active = c.Active;
            e.IsPropertyDescriptionRemoved = c.IsPropertyDescriptionRemoved;
            e.IsPropertyPlayersRemoved = c.IsPropertyPlayersRemoved;
            e.IsPropertyMascotsRemoved = c.IsPropertyMascotsRemoved;
            e.IsPropertyActiveRemoved = c.IsPropertyActiveRemoved;

            ReflectUtils.CopyPropertyValue("CommandId", c, e);


            e.CreatedBy = (string)c.RequesterId;
            e.CreatedAt = DateTime.Now;

			var version = c.Version;


            return e;
        }

        protected virtual ITeamStateDeleted Map(IDeleteTeam c)
        {
			var stateEventId = new TeamStateEventId(c.TeamName, c.Version);
            ITeamStateDeleted e = NewTeamStateDeleted(stateEventId);
			
            ReflectUtils.CopyPropertyValue("CommandId", c, e);


            e.CreatedBy = (string)c.RequesterId;
            e.CreatedAt = DateTime.Now;


            return e;
        }

        private void SetNullInnerIdOrThrowOnInconsistentIds(object innerObject, string innerIdName, object innerIdValue, string outerIdName, object outerIdValue)
        {
            if (innerIdValue == null)
            {
                ReflectUtils.SetPropertyValue(innerIdName, innerObject, outerIdValue);
            }
            else if (!Object.Equals(innerIdValue, outerIdValue))
            {
                if (innerIdValue is string && outerIdValue is string && ((string)innerIdValue).Normalize() == ((string)outerIdValue).Normalize())
                {
                    return;
                }
                throw DomainError.Named("inconsistentId", "Outer {0} {1} NOT equals inner {2} {3}", outerIdName, outerIdValue, innerIdName, innerIdValue);
            }
        }

////////////////////////

        protected TeamStateCreated NewTeamStateCreated(string commandId, string requesterId)
        {
            var stateEventId = new TeamStateEventId(_state.TeamName, ((ITeamStateProperties)_state).Version);
            var e = NewTeamStateCreated(stateEventId);

            e.CommandId = commandId;

            e.CreatedBy = (string)requesterId;
            e.CreatedAt = DateTime.Now;

            return e;
        }

        protected TeamStateMergePatched NewTeamStateMergePatched(string commandId, string requesterId)
        {
            var stateEventId = new TeamStateEventId(_state.TeamName, ((ITeamStateProperties)_state).Version);
            var e = NewTeamStateMergePatched(stateEventId);

            e.CommandId = commandId;

            e.CreatedBy = (string)requesterId;
            e.CreatedAt = DateTime.Now;

            return e;
        }


        protected TeamStateDeleted NewTeamStateDeleted(string commandId, string requesterId)
        {
            var stateEventId = new TeamStateEventId(_state.TeamName, ((ITeamStateProperties)_state).Version);
            var e = NewTeamStateDeleted(stateEventId);

            e.CommandId = commandId;

            e.CreatedBy = (string)requesterId;
            e.CreatedAt = DateTime.Now;

            return e;
        }

////////////////////////

		private TeamStateCreated NewTeamStateCreated(TeamStateEventId stateEventId)
		{
			return new TeamStateCreated(stateEventId);			
		}

        private TeamStateMergePatched NewTeamStateMergePatched(TeamStateEventId stateEventId)
		{
			return new TeamStateMergePatched(stateEventId);
		}

        private TeamStateDeleted NewTeamStateDeleted(TeamStateEventId stateEventId)
		{
			return new TeamStateDeleted(stateEventId);
		}


    }

}

